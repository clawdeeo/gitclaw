name: Main

on:
  push:
    branches: [main]

  pull_request:
    branches: [main]

jobs:
  verify:
    uses: ./.github/workflows/verify.yml

  build:
    needs: verify
    uses: ./.github/workflows/build.yml

  test:
    needs: build
    uses: ./.github/workflows/test.yml
